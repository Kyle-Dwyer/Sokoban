地图中元素：
墙外空地与墙，墙内空地，箱子，目标，玩家

操作
移动与推箱子，选关，帮助，退出

逻辑处理
用户能否移动，用户移动，用户后退，箱子能够推动，游戏是否结束，退出与帮助，选关

设计：
打印信息类：打印地图，读取icon，打印说明信息，打印提示信息（开局，地图选择，指令输入提示，结束），输入合法性判断 OK
game类：开始和结束session OK
session类：结束判断，加载地图，返回map，更新map与player OK

icon.h：宏 OK
direction.h：宏 OK
pos类：x&y,重载+，==，OK

map类：玩家，map<>，后退 OK
player类：是否可以移动, 移动,后退 OK
